# Structure-8866

Delete or override all current web files in this repository. This must be a pure native Android project, NOT a React or 

## Build Instructions

Open project in Android Studio or compile via Gradle:
```bash
./gradlew assembleDebug
```
